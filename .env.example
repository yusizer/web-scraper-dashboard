# SQLite for local dev; PostgreSQL for production if needed
DATABASE_URL=sqlite+aiosqlite:///./scrapes.db

# Request settings
REQUEST_TIMEOUT_SECONDS=15
MAX_ITEMS=100

# Web server
WEB_HOST=0.0.0.0
WEB_PORT=8000
